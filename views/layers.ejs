<% sources[sid].vector_layers.forEach(function (layer) { %>

  var layerColor = '#' + randomColor(lightColors);

  map.addLayer({
    'id': '<%= sid %>-<%= layer.id %>-polygons',
    'type': 'fill',
    'source': '<%= sid %>',
    'source-layer': '<%= layer.id %>',
    'filter': ["==", "$type", "Polygon"],
    'layout': {},
    'paint': {
      'fill-opacity': 0.1,
      'fill-color': layerColor
    }
  });

  map.addLayer({
    'id': '<%= sid %>-<%= layer.id %>-polygons-outline',
    'type': 'line',
    'source': '<%= sid %>',
    'source-layer': '<%= layer.id %>',
    'filter': ["==", "$type", "Polygon"],
    'layout': {
      'line-join': 'round',
      'line-cap': 'round'
    },
    'paint': {
      'line-color': layerColor,
      'line-width': 1,
      'line-opacity': 0.75
    }
  });

  map.addLayer({
    'id': '<%= sid %>-<%= layer.id %>-lines',
    'type': 'line',
    'source': '<%= sid %>',
    'source-layer': '<%= layer.id %>',
    'filter': ["==", "$type", "LineString"],
    'layout': {
      'line-join': 'round',
      'line-cap': 'round'
    },
    'paint': {
      'line-color': layerColor,
      'line-width': 1,
      'line-opacity': 0.75
    }
  });

  map.addLayer({
    'id': '<%= sid %>-<%= layer.id %>-pts',
    'type': 'circle',
    'source': '<%= sid %>',
    'source-layer': '<%= layer.id %>',
    'filter': ["==", "$type", "Point"],
    'paint': {
      'circle-color': layerColor,
      'circle-radius': 2.5,
      'circle-opacity': 0.75
    }
  });

  layers.polygons.push('<%= sid %>-<%= layer.id %>-polygons');
  layers.polygons.push('<%= sid %>-<%= layer.id %>-polygons-outline');
  layers.lines.push('<%= sid %>-<%= layer.id %>-lines');
  layers.pts.push('<%= sid %>-<%= layer.id %>-pts');

<% }); %>
